import { Router } from 'express';
import 'express-async-errors';


const router = Router();

//registro
router.post('/', (req, res) => {});

//login
router.get('/', (req, res) => {});


export default router;
